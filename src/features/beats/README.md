# Beats Feature Boundary

Put beat listing query builders, list enrichment adapters, and beat-specific UI helpers here.

## Modules

- `beats-query.ts` — typed URL param helpers, range-patch utilities, and quick-filter pill config for the `/beats` browse page
- `BeatsQuickFilters.tsx` — quick-filter pill row component (server component, uses `beats-query`)
